// Stub for Task 20: the tabbed panels (guide, parts, pinouts, checks, truth
// table, options) and the toolbar/legend above the board.

import type { AppState } from './state.ts';

export function renderPanels(_panels: Element, _toolbar: Element, _legend: Element, _s: AppState): void {}
